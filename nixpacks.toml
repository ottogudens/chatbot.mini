[phases.setup]
nixPkgs = ["...", "nginx"]

[phases.build]
cmds = [
    "mkdir -p /etc/nginx/conf.d",
    "echo 'client_max_body_size 500M;' > /etc/nginx/conf.d/upload_size.conf",
    "mkdir -p /app/uploads"
]
